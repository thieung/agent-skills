# Optional path overrides for recall scripts.
# Defaults work for standard Claude Code installs and align with the session-sync skill.

# Claude Code source paths (raw JSONL).
CLAUDE_HOME="$HOME/.claude"
CLAUDE_PROJECTS_DIR="$HOME/.claude/projects"

# Obsidian vault root. Used by session-graph.py to normalize vault-relative file paths.
# If unset, graph mode walks up from CWD looking for `.obsidian/`, then falls back to CWD.
VAULT_DIR=""
